<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>🦐 prawn — {{ .Repo }}</title>
{{ template "styles" . }}
</head>
<body>
<header id="top">
  <div>
    <h1>🦐 prawn — {{ .Repo }}</h1>
    <p>{{ .Total }} {{ .Noun }} across {{ len .Sections }} section{{ if gt (len .Sections) 1 }}s{{ end }} · generated {{ .GeneratedAt }}{{ if .WithAI }} · scored, surest first{{ end }}</p>
  </div>
  {{ template "theme-toggle" . }}
</header>
<main>
  <div class="toc">
    {{ range .Sections }}
    <a class="lens" href="#{{ .Slug }}">
      <h2><span class="n">prawn {{ if .Name }}{{ .Name }}{{ else }}{{ .Slug }}{{ end }}</span> <span class="count">— {{ .Total }} candidates</span></h2>
      <p class="q">{{ .Question }}</p>
      <p>{{ .Description }}</p>
      {{ range .Classes }}{{ if .Count }}<span class="pill"><span class="{{ .Kind }}">{{ .Name }}</span> <span class="c">{{ .Count }}</span></span>{{ end }}{{ end }}
    </a>
    {{ end }}
  </div>

  {{ range .Sections }}
  <section id="{{ .Slug }}">
    <h2><span class="n">prawn {{ if .Name }}{{ .Name }}{{ else }}{{ .Slug }}{{ end }}</span> <span class="count">— {{ .Total }} candidates</span></h2>
    <p class="q">{{ .Question }}</p>
    <p class="desc">{{ .Description }}</p>
    {{ if .Note }}<p class="note">{{ .Note }}</p>{{ end }}
    <p class="cmd">act on these with <code>{{ .Command }}</code></p>
    {{ if .Truncated }}<div class="trunc">showing the first {{ len .Items }} of {{ .Total }} candidates (--limit)</div>{{ end }}
    {{ if .Items }}
    <div class="items">
      {{ range .Items }}
      <div class="item">
        <h3><a href="{{ .URL }}" target="_blank" rel="noopener">#{{ .Number }}</a> {{ .Title }}</h3>
        <div class="meta">{{ .Meta }}</div>
        {{ range .Evidence }}<div class="ev">{{ range . }}{{ if .URL }}<a class="{{ .Kind }}" href="{{ .URL }}" target="_blank" rel="noopener">{{ .Text }}</a>{{ else }}<span class="{{ .Kind }}">{{ .Text }}</span>{{ end }} {{ end }}</div>{{ end }}
        {{ if .AIScore }}<div class="ai"><span class="score {{ .AIKind }}">{{ .AIScore }}</span> <span class="dim">—</span> {{ .AIReason }}</div>{{ end }}
      </div>
      {{ end }}
    </div>
    {{ end }}
    <a class="top" href="#top">↑ back to top</a>
  </section>
  {{ end }}
</main>
</body>
</html>
